rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Users collection - allow registration and login
    match /users/{userId} {
      // Allow anyone to create users (for registration)
      allow create: if true;
      // Allow authenticated users to read/write their own data
      allow read, write: if request.auth != null && request.auth.uid == userId;
      // Allow reading own data for login (temporary - should be removed in production)
      allow read: if true;
    }

    // Jobs collection - for job listings
    match /jobs/{jobId} {
      // Allow anyone to read jobs (for browsing)
      allow read: if true;
      // Allow authenticated users to create jobs
      allow create: if request.auth != null;
      // Allow users to modify only their own jobs
      allow update, delete: if request.auth != null &&
        request.auth.uid == resource.data.posterId;
    }
  }
}